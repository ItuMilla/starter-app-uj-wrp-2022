-- SQLite
select * from taxi_driver;

select * from taxi

select * from route;
        taxi_name

select * from trips;
        route_id
---join the taxi_drives with the taxi_id table


select name, drives_name, cantact_no from taxi_id 
join car_model
on car_make.id = car_model.make_id;s